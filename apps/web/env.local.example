# apps/web/env.local.example
# ローカル開発用（値は各自の環境に合わせて設定）
#
# 使い方:
# - `apps/web/.env.local` を作成し、この内容をコピーして値を埋めてください。

# API / BFF
NEXT_PUBLIC_BFF_URL=http://localhost:8787

# BFF直叩き対策（Nextサーバーのみが送れるヘッダ用。ブラウザへ露出させない）
# 本番では BFF 側の Secret `BFF_PROXY_TOKEN` と同じ値を設定してください
BFF_PROXY_TOKEN=change-me

# Supabase（Webクライアント用）
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Stripe（Web側で使う公開キー）
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxx

# Sanity（ブログ等）
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# セッション/JWT設定（必須）
# 開発環境用: 32文字以上のランダム値（例: `openssl rand -hex 32` または `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`）
# 本番環境では必ず強力な値に変更してください
JWT_SECRET=your-jwt-secret-key-change-this-in-production-min-32-chars

# ============================================
# Security Configuration
# ============================================

# セッション署名用シークレット（必須）
# API側と同じ値を設定
# 生成: openssl rand -hex 32
SESSION_SECRET=

# BFFプロキシトークン（本番では必須）
# API側と同じ値を設定
# 生成: openssl rand -hex 32
BFF_PROXY_TOKEN=

# ============================================
# Admin Security Configuration
# ============================================

# 管理者画面へのアクセスを許可するIPアドレス（カンマ区切り）
# CIDR表記可（例: 192.168.1.0/24）
# 空の場合は全IPを許可（開発環境用）
# 本番環境では必ず設定してください
ADMIN_ALLOWED_IPS=
